# Core

`environment_monitor.yaml` is a Home Assistant package. It creates configurable
temperature and humidity bands, three status sensors, and four persistent
notification automations. Copy this file into `/config/packages/` as described
in the repository root README.

It has no HACS or custom-card dependency.
